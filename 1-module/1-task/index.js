/**
 * Factorial
 * @param {number} n
 * @returns {number}
 */
function factorial(n) {
 // let num = prompt("Ведите число", );
  let a = n;
  
  let result = 1;
for ( i  = 0; i < a ; i++){
  result = result * (a - i);
  }
  returns(result);
  // alert(result);

//   let soncha = 1;
//   i = 0;  //обнуляем счетчик
// while( a  >  i ){
//   soncha = soncha * ( a - i);
//   i++;
// }
// alert(soncha);
}
